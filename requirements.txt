# Core dependencies
torch>=2.0.1           # Updated for proper Python 3.12 wheel support
transformers>=4.25.0   # Bumped to include recent compatibility fixes
datasets>=2.0.0
numpy>=1.24.0          # Updated to ensure wheel availability on Python 3.12
pandas>=1.5.3          # Increased minimum for better Python 3.12 compatibility
scikit-learn>=1.2.0    # Updated to a version known to work on Python 3.12
matplotlib>=3.6.0      # Bumped for improved Python 3.12 support
seaborn>=0.12.0        # Updated alongside matplotlib

# API and serving
fastapi>=0.95.0        # Increased to a more recent release
uvicorn>=0.18.0        # Bumped for Python 3.12 fixes
pydantic>=1.10.0       # Updated for improved compatibility
starlette>=0.21.0      # Increased to a version with Python 3.12 support
python-multipart>=0.0.6 # Minor bump to ensure the latest fixes are applied
aiofiles>=23.1.0       # Updated version recommended for async file handling on Python 3.12
httpx>=0.23.0          # Increased to leverage recent compatibility patches
redis>=4.6.0           # Bumped to the latest release for Python 3.12 support

# Data processing
nltk>=3.8.0            # Updated for enhanced Python 3.12 support
spacy>=3.5.0           # Bumped to a recent version that supports Python 3.12
beautifulsoup4>=4.11.0 # Minor update to stay current
lxml>=4.9.0           # Updated to ensure proper compatibility with Python 3.12
PyYAML>=6.0.0
jsonschema>=4.17.0     # Bumped to pick up latest bug fixes and support


# Text processing
regex>=2022.3.15       # Updated for Python 3.12 optimizations
ftfy>=6.1.0            # Minor version bump for improvements
tqdm>=4.64.0           # Updated to the latest stable release
smart-open>=6.3.0       # Increased to capture recent compatibility changes

# Monitoring and logging
prometheus-client>=0.16.0  # Bumped for more recent fixes
python-json-logger>=2.0.0
sentry-sdk>=1.9.0          # Updated to incorporate Python 3.12 compatibility patches

# Testing
pytest>=7.2.0           # Updated major release for Python 3.12 support
pytest-cov>=4.0.0       # Bumped version
pytest-mock>=3.11.0     # Increased to a recent release
requests-mock>=1.10.0   # Updated to the latest stable version

# Development tools
black>=23.3.0           # Updated to support Python 3.12
flake8>=5.0.0           # Bumped version for enhanced compatibility
isort>=5.12.0           # Updated for better integration with new Python versions
pre-commit>=2.20.0      # Increased for the latest features and fixes
mypy>=1.0.0             # Updated to leverage recent improvements
Sphinx>=5.0.0           # Bumped to ensure compatibility with Python 3.12

# Security
pyjwt>=2.6.0            # Updated for improved security and Python 3.12 support
passlib>=1.7.4          # Minor bump for compatibility improvements
cryptography>=40.0.0    # Updated to a release that supports Python 3.12 reliably
python-dotenv>=1.0.0    # Bumped for consistency with current standards
